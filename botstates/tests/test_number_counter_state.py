class MockContext():

    @property
    def author:


def test_team_assignment:
    """
        when a new user joins
        then assign them to the team with the least number of users
        in the event of a tie it is arbitratily broken
    """

    numberCounter = BotStateContext(NumberCounter(20))
    mock_user_join_ctx =
    numberCounter.handle_event_message()